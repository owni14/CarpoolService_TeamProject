<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.kh.team.mappers.complain">

	<insert id="insertComplain">
	insert into complain
		(complain_seq, admin_code, m_id, complain_content, complain_group, complain_classification, complain_is_finish)
	valuse
		(seq_complain.nextval, #{admin_code}, #{m_id}, #{complain_content}, seq_complain.nextval, #{complain_classification}, #{complain_is_finish})
	</insert>
	
	<select id="getComplainListById">
	select * from complain
	where m_id = #{m_id}
	</select>
</mapper>